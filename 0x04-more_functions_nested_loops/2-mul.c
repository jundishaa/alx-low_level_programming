#include "main.h"
#include <stdio.h>
/**
  *mul - check for it
  *@a: the first one
  *@b: the second one
  *Return: 1 for digit and 0 for anything else
  */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
