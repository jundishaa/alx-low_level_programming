#include "main.h"
/**
  *_abs - computes the absolute value
  *letter Another cases, shows 0
  *
  *@i: the input number as an intiger
  *Description: 'prints the alphabet in lower case'
  *Return: 1 is greater than zero
  *-1 is less than zero.
  */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
